version: '3'
services:
  my-justice-app:
    restart: always
    build:
      context: ../../
      dockerfile: Dockerfile
    environment:
      - VIRTUAL_HOST=https://3.133.149.241/
      - LETSENCRYPT_HOST=https://3.133.149.241/